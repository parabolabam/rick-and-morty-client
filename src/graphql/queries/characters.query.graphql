query($page: Int!, $filter: FilterCharacter!) {
  characters(page: $page, filter: $filter) {
    info {
      count
      pages
      next
      prev
    }
    results {
      name
      image
      id
    }
  }
}
